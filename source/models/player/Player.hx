package models.player;

/** Represents the player itself outside of battle. Characters, inventory, skills, money, etc. **/ 
class Player {
    public var chars:Array<CharacterInfo> = [];
    public var money:Int;
    
    public function new() {

    }
}